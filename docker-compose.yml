version: "3"
services:
  repo:
    container_name: repo
    build: .
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - .:/usr/src/app
    links:
      - mongo

  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
